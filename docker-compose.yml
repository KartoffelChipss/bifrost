services:
  bifrost:
    image: kartoffelchipss/bifrost:latest
    container_name: bifrost
    restart: unless-stopped
    environment:
      FLUXER_BOT_TOKEN_FILE: /run/secrets/fluxer_bot_token
      DISCORD_BOT_TOKEN_FILE: /run/secrets/discord_bot_token
    secrets:
      - fluxer_bot_token
      - discord_bot_token
    volumes:
      - ./config:/config

secrets:
  fluxer_bot_token:
    external: true
  discord_bot_token:
    external: true
